spring:
  cloud:
    config:
      name: ${spring.application.name},COMMON-DATA-SOURCE,COMMON-PROPERTIES,EUREKA-CLIENT-PROP,ZIPKIN-PROP,LOGGER-PROP
      uri:
      - http://cloud-config-server:8999
  application:
    name: ORDER-SERVICE

management:
  endpoints:
    enabled-by-default: true
    
