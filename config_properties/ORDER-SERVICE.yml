server:
  port: 8081

management:
  health:
    circuitbreakers:
      enabled: true
    ratelimiters:
      enabled: true
  endpoints: 
    web:
      exposure:
        include:
        - '*'
  endpoint:
    health:
      show-details: always

     
com:
  test:
    payment:
      controller: "http://PAYMENT-SERVICE/payment"