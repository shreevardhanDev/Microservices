spring:
  cloud:
    config:
      name: ${spring.application.name},common-data-source,common-properties,eureka-client-prop,security-jwt
      uri: http://cloud-config-servier
  application:
    name: security-jwt